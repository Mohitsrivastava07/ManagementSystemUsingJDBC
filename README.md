# ManagementSystemUsingJDBC
In this repository (ManagementSystemUsingJDBC), some project like Management System using Java (using OOPs for the backend develop, interfaces), JDBC (for connecting the database to java), MySQL (for CRUD operation performance) in which using SQL(to store, manipulate, retrieve data organized into tables with rows and columns)
